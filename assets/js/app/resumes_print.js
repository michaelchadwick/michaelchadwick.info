// maybe remove all links...dunno

// const links = document.querySelectorAll('a')

// links.forEach(link => {
//   const el = document.createElement('span')
//   el.textContent = link.textContent
//   link.parentNode.replaceChild(el, link)
// })
